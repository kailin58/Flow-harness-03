#查看全部的文档看那边部分还缺少,需要补全的,meeting\20260407
--------------




. 文档自身声明的缺口（总判断）
开篇已写明：业务范围已覆盖，但规则深度不足；若不当场补全，开发期容易返工。

#文末（约 3064–3066 行）还承诺可另起一稿：可开发系统功能架构清单 + 模块拆分 + 数据表主实体清单——当前文件里没有这三块，属于「未交付的续篇」。

#2. 明确标了「占位 / 待完善 / 单独立项」的段落
#位置（主题）	缺什么
#§13.1 申请入口与学习平台
#与规则学习中心串联；科目、考试、与审核顺序、未完成学习时的交互 仅占位，需产品与教研/合规专项定稿。
#§13.2 存量「自然注册」老会员
之后能否被绑定为某推广员的客：法务 + 增长单独立项（文档默认保守：无首引流则无 canonical）。
§13.x 视频素材
AI 短视频生产线：一期可占位，不阻塞主链路。
§13.9 自动收货后的退货限制
占位，需与全站售后政策统一后写死。
§13.13 分级与长周期退货
占位；推广员等级/加成、半年一年内退货与已发佣追回：成本测算后单独立项。
§14 消费积分「默认口径」（较早条目）
仍写「占位、可配置」；同章 §14.0 已对累积基数等做了较完整默认口径，建议合并删冗余或统一引用 §14.0，避免两处不一致。
§14.0 报表与税务
占位落地：财务需在①合同负债/递延收益 vs ②发放即营销费用 二选一冻结；可变对价估值字段需与审计对齐。
§14.1 会员等级权益
各等级差异化权益：先占位，只要求先落地主数据 + 等级引擎 + 积分/券叠加。
§14.0 DISTRIBUTION_REWARD（分销积分）
#默认一期不做；若要做须与现金佣互斥/隔离并公示。#




#4. 分期能力：文档写了「一期不做 / 后续再做」
#客服：一期工单 + 订单上下文 + 平台工作台；IM、机器人、知识库、满意度、质检报表 属后续。
#分销：不做多级、不做全渠道订单自动全量分佣；站外回站分销链 按渠道开关灰度；老客无链跟单、跨店阶梯、团队激励等放二期。
#第三方 IM：与自有工单/会话是以你方为准还是双写，需明确（文中已提醒，未写死方案）。
#外部渠道订单：客服是只读+备注还是与站内同一工单，需与渠道能力对齐定案。


#5. 其它「提了原则、实现要再落文档」的项
清退后未结佣金 处理：须 法务 + 财务定稿。
推广素材 / 规则页 具体文案与模板：合规定稿。
计佣基数（是否扣积分抵现等）：要求在 commission_rule_snapshot 二选一封冻，具体选哪一种需产品+财务确认。
积分抵扣后退单：链式回冲 vs 现金等价扣回，需 财务选一种全站一致。
#会员 ID 合并：§14.1.1 有较细口径，但 openid/多端合并 仍要在实现与隐私条款里落到字段级与流程级。




6. 建议的补全顺序（务实，可执行）
[P0] 参数冻结：将 §3070 起五张规则表整理为「配置项清单 + 默认值 + Owner（财务/法务/运营）」，并明确生效口径与变更流程。
[P0] 占位关闭：学习平台流程、自动收货后售后、报表税务选型、推广员分级与长周期退货，逐项给出“本期定稿”或“二期不做”的结论。
[P1] 结构补档：新增附录/新文档，补齐「功能架构清单 + 模块边界 + 主数据实体清单」（对应文末已承诺但未交付内容）。
[P1] 口径统一：合并 §14 的消费积分占位描述与 §14.0 正文，保留单一事实来源，避免条目重复和规则冲突。
结论：meeting\20260407 在架构与域划分上已较完整；真正缺的是——占位专题定稿、五张规则表的参数冻结、财务/法务/售后对齐的几条硬规则，以及文末提到的**下一版「可开发清单 + 模块 + 主表」**尚未写入本文件。以下补充一版可跟踪 checklist 作为执行基线。

7. 待补任务 checklist（可跟踪）

## A. P0 参数冻结（五张规则表）
- [ ] 产出《五张规则表配置清单 v1》：字段名、含义、取值范围、默认值、是否可前台配置。
- [ ] 为每个参数标注 Owner（财务/法务/运营/产品）与会签人。
- [ ] 明确生效机制（即时/次日/版本化）与历史追溯方案（变更记录、快照、审计字段）。
- [ ] 冻结“计佣基数是否扣积分抵现”单一口径，并写入 `commission_rule_snapshot` 说明。
- [ ] 截止日期：____-__-__；负责人：____；依赖：财务政策确认、法务合规确认。

## B. P0 占位关闭（本期定稿或二期不做）
- [ ] 学习平台流程：补齐申请入口、学习科目、考试门槛、审核前置关系、未完成学习时交互。
- [ ] 自动收货后售后：明确是否允许退货、允许时效、是否影响佣金结算与回冲。
- [ ] 报表税务选型：冻结“合同负债/递延收益”或“发放即营销费用”二选一。
- [ ] 推广员分级与长周期退货：给出一期规则，或明确写入“二期不做”并标注触发条件。
- [ ] 截止日期：____-__-__；负责人：____；依赖：财务、法务、售后联合评审。

## C. P1 结构补档（承诺内容补齐）
- [ ] 新增《可开发功能架构清单》：按域列能力、输入输出、约束与验收口径。
- [ ] 新增《模块边界说明》：会员、分销、订单、售后、积分、客服、结算边界与责任。
- [ ] 新增《主数据实体清单》：主键、唯一性、状态机、关联关系、归档策略。
- [ ] 在 `meeting\20260407` 文末增加链接，指向以上三份新文档。
- [ ] 截止日期：____-__-__；负责人：____；依赖：架构评审会。

## D. P1 口径统一（§14 与 §14.0）
- [ ] 删除或改写 §14 中“消费积分占位”重复段，统一引用 §14.0 作为单一事实来源。
- [ ] 校验“积分累积基数、抵扣、退单回冲”三项在全文仅保留一套规则表述。
- [ ] 增加“版本号 + 更新日期 + 审核人”页脚，避免后续再次分叉。
- [ ] 截止日期：____-__-__；负责人：____；依赖：产品文档 owner 最终审校。

8. 验收标准（Definition of Done）
- [ ] 所有“占位/待补”标签清零，或均标注“二期不做 + 原因 + 触发条件”。
- [ ] 五张规则表参数完成会签，系统可按默认值直接落库与上线。
- [ ] 架构清单、模块边界、主数据实体三份文档均已发布并被主文档引用。
- [ ] §14 与 §14.0 无冲突描述，抽检 10 条规则一致率 100%。