# Flow Harness 架构升级总结
**日期**：2026-04-12  
**性质**：架构设计沟通记录 + 决策结论  
**状态**：待实施  
**最后更新**：工作流从6层升级为11层，权限管理独立成层

---

## 四大主题索引

| 文件 | 主题 |
|---|---|
| [01_3层升4层架构.md](./01_3层升4层架构.md) | CEO + VP层 + 总监 + 子Agent |
| [02_模型管理.md](./02_模型管理.md) | 模型接入 / 路由 / 计费监控 |
| [03_Agent协作保障.md](./03_Agent协作保障.md) | 状态机 / 看板 / 协作保障 |
| [04_长期架构.md](./04_长期架构.md) | 六边形架构 / 平台化 / 协议支持 |
| [05_9层工作流设计.md](./05_9层工作流设计.md) | 工作流9层全设计 |

---

## 核心决策一览

### 组织架构（横向）
- Agent层级：3层 → 4层（新增 VP 副总层）
- VP 层由平台统一治理（共11个VP位，不允许企业新增VP）
- 每个VP默认5个总监（平台标准骨架）
- 企业可在既有VP下增加总监（受权限与配额约束）
- 总监下子Agent数量为 N（按任务负载弹性扩缩）
- 组织能力树写死：**VP → 多总监 → 多子Deep Agent → 多Skills**
- 治理约束写死：Skills 调用受 L2/L5 约束；子Deep Agent输出必须经 L7/L8；进化只在 L11、上线只经 L10
- COMM_RULES 从"基于Agent ID"改为"基于level"

### 工作流（纵向）★ 本次更新
- 工作流从 **6层升级为11层**
- 新增 Layer 1（入口扩展）、Layer 2（权限管理）、Layer 4（协作编排）、Layer 10（版本发布）、Layer 11（进化实验）
- 权限管理**独立成层**（Layer 2），不与安全策略混用
- 详见：[05_9层工作流设计.md](./05_9层工作流设计.md)

### 模型
- 三种来源：平台内置 / BYOK / 本地模型
- 不引入重框架，使用官方SDK
- 配置走 `.flowharness/models.json` + 环境变量引用Key
- 模型分配/调度归属 **L3 任务编排层**（路由、降级、成本与SLA权衡）
- L6 仅负责执行期监控与反馈，L4 仅负责多Agent协作冲突协调（不承载模型分配主责）

### 协作
- 补充 Task State Machine（最缺的一环）
- 添加 Agent 看板（终端版先做，Web版后做）

### 长期
- 六边形架构（Ports & Adapters）
- MCP 协议先接，A2A 占位
- 平台化方向：框架 → 平台 → 生态

---

## 11层工作流全景

```
┌─────────────────────────────────────────────────────┐
│  L1  入口扩展层   【预留插件】网关/请求归一化         │
│  L2  权限管理层   【独立】RBAC/租户/操作授权          │ ← 新增
├─────────────────────────────────────────────────────┤
│  L3  任务编排层   分析/拆解/分配VP（原L1）            │
│  L4  协作编排层   状态机/VP协调/冲突检测              │ ← 新增
│  L5  安全策略层   准入/合规检查（原L2）               │
│  L6  执行监控层   Monitor/AutoRetry（原L3）           │
├─────────────────────────────────────────────────────┤
│  L7  Inspector层  质检/审查（原L4）                  │
│  L8  质量门禁层   通过/拦截（原L5）                   │
│  L9  反馈闭环层   复盘/优化（原L6）                   │
│  L10 版本发布层   候选/审批/灰度/回滚                 │ ← 新增
│  L11 进化实验层   沙盒自优化/实验评测/隔离            │ ← 新增
└─────────────────────────────────────────────────────┘
```

**两维度说明：**
```
横向（组织层级）：CEO → VP（平台固定11位）→ 总监（企业可扩）→ 子Agent（N）
纵向（工作流）  ：入口 → 权限 → 编排 → 协作 → 安全
                  → 执行 → 质检 → 门禁 → 闭环 → 发布 → 进化（11层）
两个维度正交，互不冲突
```

---

## 实施优先级

```
P0 基础（先做，其他依赖它）：
  ① 六边形架构骨架 + 5个端口接口定义
  ② COMM_RULES 改为基于 level（支持VP扩展）
  ③ VP层骨架 + 数字VP写死
  ④ Task State Machine（L4协作编排层核心）
  ⑤ Agent看板（终端版）
  ⑥ L1/L2 入口+权限层占位（插件链空实现）

P1 模型管理：
  ⑦ model-registry + model-router
  ⑧ 官方SDK接入 + local-adapter（Ollama）
  ⑨ cost-controller + model-monitor

P2 扩展能力：
  ⑩ L2权限管理层完整实现（RBAC + 多租户）
  ⑪ 用户自建VP Schema校验 + 模板库
  ⑫ MCP协议适配器

P3 页面（最后）：
  ⑬ Web Agent看板
  ⑭ 模型管理页面
  ⑮ VP配置页面
  ⑯ 用量/账单页面
```

---

## 子层规划（系统预设不污染）

目标：用户Agent可进化，系统预设保持稳定且不被自动污染。

### 层级结构（含子层）

```
L1 入口扩展层
L2 权限管理层
  ├── L2-SYS   （系统预设身份与权限域）
  └── L2-USER  （用户Agent身份与权限域）
L3 任务编排层
L4 协作编排层
L5 安全策略层
L6 执行监控层
  ├── L6-SYS   （系统执行监控与告警）
  └── L6-USER  （用户执行监控与告警）
L7 Inspector层
L8 质量门禁层
L9 反馈闭环层
L10 版本发布层
  ├── L10-SYS-RELEASE   （系统预设发布通道）
  └── L10-USER-RELEASE  （用户Agent发布通道）
L11 进化实验层
  └── L11-USER-EVO      （用户自优化沙盒）
```

### 子层优先级（先规划）

1) **L2 权限管理层（第一优先）**
- 规划子层：`L2-SYS` / `L2-USER`
- 作用：先切清身份与作用域边界（谁能做什么）。

2) **L11 进化实验层（第二优先）**
- 规划子层：`L11-USER-EVO`
- 作用：仅用户侧进入自优化沙盒，系统预设默认不进化。

3) **L10 版本发布层（第三优先）**
- 规划子层：`L10-SYS-RELEASE` / `L10-USER-RELEASE`
- 作用：统一发布门禁，任何进化结果先过评测+审批再发布。

4) **L6 执行监控层（第四优先）**
- 规划子层：`L6-SYS` / `L6-USER`
- 作用：运行态隔离监控、限流、告警与审计分治。

### 核心原则（先写死）
- 进化只在用户隔离域发生，不允许直接写入系统预设域。
- 系统预设只接受发布层合入，不接受实验层直连。
- 任何发布必须可追踪、可审批、可回滚。

---

## 企业级能力分层归属（标准口径）

- **L1 入口扩展层**：注册/登录入口、请求归一化、租户识别、限流防刷。
- **L2 权限管理层**：角色体系、指挥权分配、租户与组织隔离、审批权限。
- **L3 任务编排层**：任务拆解、Agent分配、模型路由与调度策略。
- **L4 协作编排层**：多指挥者协调、跨Agent通信、冲突仲裁、状态机推进。
- **L5 安全策略层**：数据访问策略执行、合规检查、敏感操作拦截（deny-by-default）。
- **L6 执行监控层**：运行监控、告警、重试、熔断、分域监控（SYS/USER）。
- **L7 Inspector层**：目标对齐检查、完整性验证、质量评分、风险扫描。
- **L8 质量门禁层**：通过/打回决策，不达标结果不得进入发布链路。
- **L9 反馈闭环层**：复盘沉淀、经验回写、优化建议管理。
- **L10 版本发布层**：候选版本、审批、灰度发布、正式发布、回滚。
- **L11 进化实验层**：用户自优化沙盒、实验评测、进化结果提案（不可直达生产）。

---

## Deep Agent 分层归属与治理链

定位：Deep Agent 是执行能力体，不是总框架；总框架仍由11层治理链路承载。

- **主归属层：L3 任务编排层**
  - 负责 Deep Agent 的分配、路由、降级和成本/SLA权衡。
- **协同层：L4 协作编排层**
  - 负责多 Deep Agent 并行协作、冲突仲裁和状态推进。
- **运行层：L6 执行监控层**
  - 负责运行健康、时延、成本、失败重试与熔断反馈。
- **治理链：L7 -> L8 -> L10 -> L11**
  - L7 质量检查
  - L8 门禁决策
  - L10 发布与回滚
  - L11 进化实验（仅用户隔离域）

执行红线：
- Deep Agent 不得绕过 L2/L5 权限与安全策略。
- Deep Agent 输出不得绕过 L7/L8 直接进入生产。
- 进化结果不得直写生产，必须经 L10 发布链路。

---

## Skills 生命周期与分层约束

定位：Skills 为平行能力插件，由 Agent 挂载与调用，不构成新的组织层级。

生命周期：
- 注册（Registry）-> 沙盒验证（L11）-> 质量门禁（L8）-> 发布（L10）-> 运行监控（L6）-> 回滚/下线

分层约束：
- **L3**：Skills 路由与调用策略（由编排统一调度）。
- **L2**：Skills 调用权限控制（角色/租户/作用域）。
- **L5**：Skills 运行安全策略（命令/文件/API 白名单）。
- **L8/L10**：Skills 发布门禁、版本发布与回滚。
- **L11**：用户新增或变更 Skills 的实验与验证域。

治理要求：
- 用户可新增 Skills，但仅在 USER 域生效，不得污染 SYS 预设域。
- Skills 必须版本化、可审计、可回滚。
- 高风险 Skills 默认拒绝，需显式审批放行。

---

## 长期维护边界（执行准则）

为保证平台可持续演进且不失控，长期维护遵循以下五个边界：

1) **组织边界**
- VP 层平台固定治理（企业不可新增VP）。
- 企业可在既有VP下扩总监；总监下子Agent按N弹性扩缩。

2) **分层边界**
- L2/L5/L8/L10/L11 为核心治理链路，不可绕过。
- 任意能力扩展不得破坏分层职责边界。

3) **扩展边界**
- 用户可新增 Skills，但仅在 USER 域生效。
- Skills 调用必须受 L2 权限与 L5 安全策略约束。

4) **质量边界**
- 新能力默认先在 L11 沙盒验证，再经 L8/L10 门禁发布。
- 任何上线能力必须支持版本化与可回滚。

5) **审计边界**
- 全链路行为必须可追踪、可回放、可追责。
- 高风险动作必须有审批记录与执行留痕。

---

## 层级补全清单（原有/需更新/新增）

范围：仅针对“层级治理”继续完善项，不涉及代码实现。

### 1) 责任矩阵落表（Owner / 输入 / 输出 / SLO / 失败处理）

**原有：**
- 已有11层职责主描述（层级结构与职责边界已明确）。
- 已有部分输入/输出描述，但分散在不同文档中。

**需要更新：**
- 将每层职责统一落为标准矩阵字段：`Owner / 输入 / 输出 / SLO / 失败处理`。
- 补齐 `L10/L11` 的SLO与失败处理口径（当前偏原则描述）。

**新增：**
- 新增《11层责任矩阵总表》（一页可审阅版本）。
- 新增 Owner 默认归属规则（平台侧/租户侧/自动化流程）。

### 2) 跨层调用白名单（允许关系 + 禁止隐式穿透）

**原有：**
- 已有“核心治理链路不可绕过”原则（L2/L5/L8/L10/L11）。
- 已有“模型分配在L3，L4/L6协同”的职责边界共识。

**需要更新：**
- 将原则转成“可执行调用矩阵”：明确各层可调用路径与调用方向。
- 明确禁止路径（如实验层直达生产、编排层绕过门禁等）。

**新增：**
- 新增《跨层调用白名单表》（Allowed / Denied / Needs-Approval 三态）。
- 新增“隐式穿透告警规则”（出现未授权跨层即记录并告警）。

### 3) 变更分类机制（仅编号变化 / 职责变化 / 新增能力）

**原有：**
- 已有“仅改编号不改职责”的沟通共识。
- 已有部分新旧层映射描述，但缺少统一标记规范。

**需要更新：**
- 在总览中固定“变更分类说明”章节，后续改动统一打标签。
- 对关键层级调整补齐分类标记，避免评审误解为重构。

**新增：**
- 新增三类标准标签：
  - `仅编号变化`
  - `职责变化`
  - `新增能力`
- 新增《变更记录模板》：影响层、分类、原因、风险、回滚点。

---

## Skills 治理补全（关键优先）

### 必补项（写入治理标准）

1) **Skill Registry**
- 必填字段：`id / version / owner / scope / risk_level`。

2) **Skill 门禁流程**
- 标准链路：开发 -> 沙盒验证（L11）-> 质量门禁（L8）-> 发布（L10）。

3) **Skill 安全策略**
- 文件/命令/API 权限最小化。
- 默认拒绝，按白名单显式放行。

4) **Skill 兼容策略**
- 每个 Skill 必须声明依赖的 Agent 类型与最低平台版本。

5) **Skill 回滚机制**
- 每次升级必须支持快速回退到上个稳定版本。

### 状态标记（原有/需更新/新增）

**原有：**
- 已有 Skills 生命周期与分层约束（L2/L3/L5/L8/L10/L11）描述。
- 已有“用户可新增 Skills，但不可污染 SYS 域”的治理原则。

**需要更新：**
- 将 Registry 字段从描述性规则升级为强制校验项。
- 将门禁链路从原则升级为发布前必经流程。
- 将兼容策略与回滚要求纳入发布准入条件。

**新增：**
- 新增《Skill 注册规范（字段与校验）》。
- 新增《Skill 发布门禁清单》与《Skill 回滚SOP》。

---

## 下一步三项高价值产物（不写代码版）

1) **Layer × Agent × Skill 三维权限矩阵（1张表）**
- 用途：一次性明确“谁在什么层可调用什么能力”。

2) **Agent/Skill 生命周期状态机（2张图）**
- 用途：统一创建、验证、发布、回滚、下线流程。

3) **上线门禁清单（发布前10条必须通过项）**
- 用途：把门禁从口头规则变成可执行检查单。

---

## 第三维度：数据生命周期管理

> **补充日期**：2026-04-13  
> **性质**：架构补全（不修改现有11层，不修改组织层级，正交补充）  
> **结论**：现有两维（组织横向 + 工作流纵向）缺少数据管理维度，需补充四类存储库

---

### 背景：为什么需要第三维度

现有11层是**纵向工作流**，解决"任务如何流转"。  
以下8类数据贯穿整个任务生命周期，跨越多个工作流层，没有专属归属：

| 数据条目 | 现有层覆盖 | 覆盖程度 |
|---------|-----------|---------|
| 用户需求 | L1接收 + L3解析 | ⚠️ 有接收无持久化存储 |
| 任务目标 | L3拆解分配 | ⚠️ 运行时存在，无持久化 |
| 中间结果 | L6捕获 + L7检查 | ❌ 无专属存储层 |
| 规则约束 | L2权限 + L5安全 | ⚠️ 规则分散两层，无统一注册表 |
| 工具输出 | L6收集 + L7质检 | ⚠️ 流过即消，无结构化归档 |
| 审批状态 | L8门禁 + L10发布 | ⚠️ 两处分散，无统一审批状态链 |
| 异常记录 | L6告警/重试 | ❌ 只有实时告警，无持久异常库 |
| 修改历史 | L10版本 + L9复盘 | ❌ 无全链路变更审计轨迹 |

**结论**：缺的不是工作流层，是数据的归属与治理。

---

### 三维度正交模型

```
现有两维：
  横向（组织）：CEO → VP → 总监 → 子Agent
  纵向（流程）：L1 → L2 → ... → L11

新增第三维：
  数据层（Data）：S1上下文库 / S2制品库 / S3审计库 / S4规则库
```

三个维度互不干扰，任意一层均可按权限读写对应存储库。

---

### S1：上下文库（Context Store）

**管理对象**：用户需求 / 任务目标 / 执行计划 / 会话上下文 / 依赖关系图

| 字段 | 说明 |
|------|------|
| 写入时机 | L1接收需求 → L3拆解目标与执行计划 → L4生成依赖关系图 |
| 读取方 | CEO / VP / 总监 / L7 Inspector（目标对齐检查） |
| 生命周期 | 任务创建 → 任务归档，归档后只读 |
| 关键约束 | 同一任务的上下文版本化存储，支持多轮追溯 |

---

### S2：制品库（Artifact Store）

**管理对象**：中间结果 / 工具输出 / 资源占用 / 回滚快照

| 字段 | 说明 |
|------|------|
| 写入时机 | L6每步执行完成后写入；L10/L11写入回滚快照 |
| 读取方 | L7 Inspector（质检）/ L8 门禁（决策）/ L9 复盘 / 账单系统 |
| 生命周期 | 步骤完成即写入，任务结束后保留N天，快照永久保留 |
| 关键约束 | 不可覆盖写，只能追加；每条记录绑定任务ID + 层级 + Agent ID |

---

### S3：审计库（Audit Store）

**管理对象**：审批状态 / 异常记录 / 修改历史 / 并发锁状态 / 技能调用记录

| 字段 | 说明 |
|------|------|
| 写入时机 | 审批状态 → L8/L10每次状态流转；异常 → L6告警/重试/熔断时；修改历史 → 任何层对规则/配置/Agent的变更；技能调用 → L5/L6每次Skills执行 |
| 读取方 | 看板 / 合规审查 / 回滚决策 / L4协作编排（并发锁） |
| 生命周期 | **永久保留，不可删除，只可归档** |
| 关键约束 | 写入不可拒绝（即使主流程失败也必须写审计）；只有审计管理员可归档 |

---

### S4：规则库（Rules Registry）

**管理对象**：规则约束（统一L2权限规则 + L5安全规则 + L8门禁标准）

| 字段 | 说明 |
|------|------|
| 写入时机 | 平台初始化 / 管理员变更 / L10发布新规则版本 |
| 读取方 | L2权限管理层 / L5安全策略层 / L8质量门禁层（执行时查询） |
| 生命周期 | 版本化存储，旧版本保留；变更自动触发S3审计记录 |
| 关键约束 | 规则变更必须经L10发布流程，不允许直接覆盖生产规则 |

---

### 数据层全景总览

```
S1 上下文库（Context Store）
   ├── 用户需求          ← L1 写入
   ├── 任务目标          ← L3 写入
   ├── 执行计划          ← L3/L4 写入
   ├── 会话上下文        ← 全程追加
   └── 依赖关系图        ← L4 写入

S2 制品库（Artifact Store）
   ├── 中间结果          ← L6 写入，L7 读取
   ├── 工具输出          ← L6 写入，L7 读取
   ├── 资源占用          ← L6 写入，账单系统读取
   └── 回滚快照          ← L10/L11 写入

S3 审计库（Audit Store）【不可删除】
   ├── 审批状态          ← L8/L10 写入
   ├── 异常记录          ← L6 写入
   ├── 修改历史          ← 所有层变更触发
   ├── 并发锁状态        ← L4 写入，L4/L6 读取
   └── 技能调用记录      ← L5/L6 写入

S4 规则库（Rules Registry）【版本化】
   └── 规则约束          ← 管理员/L10 写入，L2/L5/L8 读取
```

---

### 写入权限矩阵（数据层 × 工作流层）

| 存储库 | 可写入的层 | 可读取的层 | 备注 |
|-------|-----------|-----------|------|
| S1 上下文库 | L1 / L3 / L4 | L3-L9 全层 / 看板 | CEO/VP 可只读 |
| S2 制品库 | L6 / L10 / L11 | L7 / L8 / L9 / 账单 | 追加不可覆盖 |
| S3 审计库 | L4 / L5 / L6 / L8 / L10 | 看板 / 合规 / L4 | 永久保留 |
| S4 规则库 | 管理员 / L10 | L2 / L5 / L8 | 变更必须过L10 |

---

### 与六边形架构的关系

四类存储库均挂载在**Storage Port**下，作为适配器实现：

```
Storage Port（存储端口）
  ├── ContextAdapter    → S1 上下文库
  ├── ArtifactAdapter   → S2 制品库
  ├── AuditAdapter      → S3 审计库（写入永不拒绝）
  └── RulesAdapter      → S4 规则库（版本化）
```

内核（CEO + VP层 + 通信规则 + 编排逻辑）永远不直接访问存储，  
所有读写通过 Storage Port 隔离，存储介质（文件/SQLite/云DB）可随时替换。

---

### 待补充产物（不写代码）

1) **S1-S4 数据字段规范表**（每类库的必填字段 + 类型 + 校验规则）

2) **数据生命周期状态机**（创建 → 活跃 → 归档 → 只读 的完整流转）

3) **跨维度访问控制矩阵**（Layer × Storage 的读写权限表，补充到跨层调用白名单中）

---

## 工具接入层（Tool Integration Layer）

> **补充日期**：2026-04-13  
> **性质**：架构扩展——Tool Port 的完整规划  
> **核心命题**：未来最关键的能力不是"模型会不会答"，而是"系统能不能拿到正确上下文并执行正确动作"

---

### 定位：在六边形架构中的位置

工具接入层是**Tool Port 的具体实现集合**，是 Agent 与外部世界之间的标准化桥梁。

```
内核（CEO + VP + 编排逻辑）
        ↓ 只能通过 Tool Port 调用
   ┌────────────────────────┐
   │      Tool Port         │  ← 统一接口契约，内核永不直连外部
   └────────────┬───────────┘
                ↓
   ┌────────────────────────────────────────────────┐
   │              工具接入层（Tool Adapters）         │
   │  T1文件  T2知识库  T3数据库  T4企业系统          │
   │  T5 API  T6搜索   T7表格   T8自动化平台         │
   └────────────────────────────────────────────────┘
                ↓
        外部真实系统
```

**关键原则**：适配器变化不影响内核；内核升级不破坏适配器；工具故障不传导到 Agent 层。

---

### 8类工具适配器规划

按接入特性分为四组：

#### 第一组：本地资源类

**T1 文件系统适配器（FileSystemAdapter）**

| 项目 | 说明 |
|------|------|
| 覆盖能力 | 读写本地/容器文件、目录遍历、文件监听、临时文件管理 |
| 核心风险 | 路径穿越攻击、意外删除、写入生产配置 |
| 权限策略 | 白名单目录制（Deny-by-Default），读/写/删 三级独立授权 |
| 失败处理 | 不存在返回空而非报错；写失败必须写 S3 审计记录 |
| 与现有层关系 | L5 安全策略层负责路径白名单校验；S2 制品库归档工具输出 |

---

#### 第二组：知识检索类

**T2 知识库适配器（KnowledgeBaseAdapter）**

| 项目 | 说明 |
|------|------|
| 覆盖能力 | 向量检索、全文检索、文档片段召回、知识图谱查询 |
| 核心价值 | 为 Agent 提供领域专有上下文，解决模型训练数据时效问题 |
| 权限策略 | 按知识域隔离（租户A的知识库不可被租户B检索） |
| 质量机制 | 召回结果置信度低于阈值时，标记为"低置信参考"而非直接使用 |
| 与现有层关系 | 召回结果写入 S1 上下文库供全链路使用；L7 Inspector 做来源核查 |

**T6 搜索引擎适配器（SearchAdapter）**

| 项目 | 说明 |
|------|------|
| 覆盖能力 | 互联网实时搜索、内网搜索、代码库搜索、文档搜索 |
| 核心风险 | 搜索结果噪音、幻觉注入、敏感信息泄露给外部搜索引擎 |
| 权限策略 | 外网搜索需显式开启；内网搜索按角色隔离 |
| 失败处理 | 搜索超时返回空集，不阻断主流程；结果必须标注来源与时间戳 |
| 与现有层关系 | 结果写入 S1 上下文库；L5 安全策略层过滤敏感查询词 |

---

#### 第三组：数据存取类

**T3 数据库适配器（DatabaseAdapter）**

| 项目 | 说明 |
|------|------|
| 覆盖能力 | 关系型DB查询/写入、NoSQL读写、时序数据库、向量数据库 |
| 核心风险 | SQL注入、意外全表删除、跨租户数据污染 |
| 权限策略 | 只读/读写/管理三级；DDL操作（建表/删表）默认禁止 |
| 操作约束 | 写操作必须在事务内；影响行数超过阈值需二次确认 |
| 与现有层关系 | 写操作触发 S3 审计记录；L8 门禁可设置数据变更审批规则 |

**T7 表格系统适配器（SpreadsheetAdapter）**

| 项目 | 说明 |
|------|------|
| 覆盖能力 | Excel/Google Sheets 读写、数据透视、公式计算、图表生成 |
| 典型场景 | 财务报表生成、数据分析输出、业务指标汇总 |
| 权限策略 | 按文件/Sheet/单元格区域三级授权 |
| 失败处理 | 格式异常时返回原始数据而非报错；写失败保留草稿 |
| 与现有层关系 | 输出文件写入 S2 制品库；版本变更触发 S3 修改历史 |

---

#### 第四组：外部系统类

**T4 企业系统适配器（EnterpriseAdapter）**

| 项目 | 说明 |
|------|------|
| 覆盖能力 | ERP、CRM、HRM、OA审批、工单系统、财务系统 |
| 核心特点 | 这类系统的操作往往**不可逆**（如财务记账、合同签署） |
| 权限策略 | 读取默认允许；写入/审批操作强制走 L8 门禁 + L10 审批链 |
| 幂等要求 | 所有写操作必须携带幂等Key，防止网络重试导致重复提交 |
| 与现有层关系 | 操作结果写 S3 审计库；审批状态同步到 S3 审批状态链 |

**T5 API服务适配器（APIAdapter）**

| 项目 | 说明 |
|------|------|
| 覆盖能力 | REST API、GraphQL、gRPC、Webhook 接收、OAuth 授权流 |
| 核心风险 | 密钥泄露、频率超限、第三方服务不稳定传导到内部 |
| 权限策略 | API Key 存 S4 规则库（不硬编码）；调用范围白名单制 |
| 稳定性机制 | 熔断器（连续失败N次则暂停调用）+ 降级响应策略 |
| 与现有层关系 | L6 执行监控层负责熔断状态管理；调用记录写 S3 审计库 |

**T8 自动化平台适配器（AutomationAdapter）**

| 项目 | 说明 |
|------|------|
| 覆盖能力 | RPA机器人调度、Zapier/n8n工作流触发、定时任务、消息队列 |
| 核心特点 | 触发后执行异步，结果需要回调或轮询获取 |
| 权限策略 | 触发权限与结果读取权限分离；高影响自动化需 L8 门禁审批 |
| 异步机制 | 触发时生成追踪ID写 S1 上下文库；回调结果写 S2 制品库 |
| 与现有层关系 | 异步状态变更触发 S3 审计；L4 协作编排层管理异步等待状态 |

---

### 工具注册表（Tool Registry）

所有工具适配器上线前必须在注册表中登记，注册表存储在 **S4 规则库**。

**必填字段：**

| 字段 | 说明 |
|------|------|
| `tool_id` | 全局唯一标识 |
| `category` | T1-T8 分类 |
| `risk_level` | 低/中/高/极高（影响调用审批策略） |
| `allowed_callers` | 允许调用的 Agent 层级（level）列表 |
| `operations` | 支持的操作列表及各操作的读/写/管理级别 |
| `rate_limit` | 频率限制（次/分钟） |
| `timeout_ms` | 超时阈值 |
| `fallback` | 超时或失败时的降级策略 |
| `idempotent` | 是否幂等（影响重试策略） |
| `audit_required` | 是否强制写 S3 审计（高风险操作必须为 true） |

---

### 工具调用标准链路

Agent 发起工具调用的完整路径：

```
Agent（任意层级）
  │
  ├─ 1. 查询工具注册表（S4）→ 确认工具存在且版本有效
  │
  ├─ 2. L2 权限检查 → 调用方 level 是否在 allowed_callers 中
  │
  ├─ 3. L5 安全策略检查 → 操作是否在白名单，参数是否合规
  │
  ├─ 4. 风险等级判断
  │     低/中风险 → 直接执行
  │     高风险    → 需 L8 门禁审批后执行
  │     极高风险  → 需 L10 发布链路人工审批
  │
  ├─ 5. Tool Port → 路由到对应 Adapter → 调用外部系统
  │
  ├─ 6. 结果处理
  │     成功 → 输出写 S2 制品库 → 返回 Agent
  │     失败 → 写 S3 异常记录 → 触发 L6 重试/熔断逻辑
  │
  └─ 7. 审计写入（risk_level=高时必须）→ S3 审计库
```

---

### 工具治理规则

#### 分级审批策略

| 风险等级 | 典型操作 | 审批要求 |
|---------|---------|---------|
| 低 | 只读查询、搜索、知识库检索 | 直接执行，事后审计 |
| 中 | 文件写入、数据库写入、API调用 | L5 合规检查通过即可执行 |
| 高 | 企业系统写入、批量数据变更 | L8 门禁审批，同步通知责任人 |
| 极高 | 财务操作、合同签署、生产DB删除 | L10 人工审批，双人确认，留痕 |

#### 稳定性治理

| 机制 | 说明 | 管理层 |
|------|------|-------|
| 熔断器 | 连续失败超阈值暂停调用，自动恢复检测 | L6 执行监控 |
| 限流 | 按工具/租户/Agent 三级限流 | L2 权限层 |
| 超时隔离 | 工具超时不阻断主流程，降级返回 | L6 执行监控 |
| 重试策略 | 仅幂等操作允许自动重试，指数退避 | L6 执行监控 |
| 版本管理 | 适配器变更必须版本化，旧版本保留 | L10 版本发布 |

---

### 工具接入层与三维度的关系汇总

| 维度 | 关系说明 |
|------|---------|
| **工作流层（纵向）** | L2 做权限校验；L5 做安全检查；L6 做熔断/重试；L8 做高风险审批；L10 管适配器版本发布 |
| **组织层（横向）** | CEO/VP 不直接调用工具；总监/子Agent 经 Tool Port 调用；调用权限受 allowed_callers 约束 |
| **数据层（S1-S4）** | 工具输出 → S2；调用记录/异常 → S3；注册表/权限规则 → S4；检索结果 → S1 |

---

### MCP 协议作为工具接入标准

**现状**：Tool Port 当前为内部定义接口。  
**方向**：MCP（Model Context Protocol）作为外部工具接入的标准化协议优先落地。

```
MCP 协议的价值：
  外部系统只需实现 MCP Server → 自动接入所有 MCP-兼容的 Agent
  内部 Agent 只需调用 MCP Client → 无感知切换后端工具

接入路径：
  Phase 1（当前）：Tool Port 内部接口，Adapter 逐一手写
  Phase 2（中期）：MCP Adapter 统一接入，支持 MCP Server 热注册
  Phase 3（长期）：工具市场（任何人发布 MCP Server 即可被发现和使用）
```

---

### 待补充产物（工具接入层）

1) **T1-T8 适配器接口规范**（统一的 Tool Port 契约：输入/输出/错误格式）

2) **工具注册表模板**（标准注册表字段填写示例，覆盖8类工具各一份）

3) **工具调用风险评级标准**（如何判定一个操作属于低/中/高/极高风险）

4) **MCP Server 接入指南**（外部系统如何以 MCP 协议接入 Tool Port）
