# Phase G 多端并行调度总表

**制定日期**: 2026-04-12
**前置条件**: Phase A-F 全部完成
**预计总时间**: 3-4 小时（3端并行）

---

## 一、Step 划分与依赖关系

| Step | 名称 | 核心产出 | 依赖 |
|------|------|----------|------|
| **G1** | ParallelExecutor 并行执行 | `src/parallel-executor.js` | 无 |
| **G2** | PipelineExecutor 流水线门控 | `src/pipeline-executor.js` | 无 |
| **G3** | KnowledgeBase Spec 扩展 | `src/knowledge-base.js` 扩展 | 无 |
| **G4** | 外部技能注册 | `.flowharness/skills/registry.json` 扩展 | 无 |

---

## 二、依赖关系图

```
                    ┌────────────────────────────────────────────┐
                    │            可 并 行 执 行                   │
                    ├────────┬────────┬────────┬────────────────┤
                    │        │        │        │                │
                    ▼        ▼        ▼        ▼                │
                ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐              │
  Wave 1        │  G1  │ │  G2  │ │  G3  │ │  G4  │              │
  (并行4个)     └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘              │
                   │        │        │        │                   │
                   │        │        │        │                   │
                   └────────┴────────┴────────┴───────────────────┘
                                      │
                                      ▼
                            ┌──────────────────┐
                            │    全量回归测试    │
                            │   npm test        │
                            └──────────────────┘
```

**结论**: G1-G4 完全独立，可以 4 端同时开工！

---

## 三、三端并行推荐分工

### 方案：4 端同时开工（最快，约 3-4 小时完成全部）

| 时段 | 端1 (Cursor) | 端2 (Claude Code) | 端3 (Codex) | 端4 (其他) |
|------|-------------|-------------------|------------|-----------|
| **全时段** | **G1** ParallelExecutor | **G2** PipelineExecutor | **G3** Spec 扩展 | **G4** 外部技能 |
| | ~90 min | ~120 min | ~60 min | ~30 min |

### 文件修改矩阵

| 文件 | G1 | G2 | G3 | G4 | 冲突风险 |
|------|----|----|----|----|----------|
| `src/parallel-executor.js` | ✅新增 | - | - | - | 无 |
| `src/pipeline-executor.js` | - | ✅新增 | - | - | 无 |
| `src/knowledge-base.js` | - | - | ✅修改 | - | 无 |
| `src/supervisor-agent.js` | ✅修改 | ✅修改 | - | - | ⚠️ 需协调 |
| `src/cli.js` | ✅修改 | - | - | ✅修改 | ⚠️ 需协调 |
| `.flowharness/skills/registry.json` | - | - | - | ✅修改 | 无 |
| `.flowharness/config.yml` | ✅修改 | ✅修改 | ✅修改 | ✅修改 | ⚠️ 需协调 |

### 冲突协调策略

| 冲突文件 | 解决方案 |
|----------|----------|
| `supervisor-agent.js` | G1 负责添加 `step4_execute_parallel`，G2 负责添加 `handleTask_v2` 和 `handleTask_pipeline`，互不干扰 |
| `cli.js` | G1 添加 `mode` 命令，G4 添加 `clone` 和 `external-skills` 命令，互不干扰 |
| `config.yml` | 合并到统一位置，各自添加自己的配置段 |

---

## 四、每个 Step 的边界

### G1: ParallelExecutor 边界

```javascript
// 输入边界
interface G1Input {
  tasks: Task[];
  executor: AgentExecutor;
  context: ExecutionContext;
}

// 输出边界
interface G1Output {
  success: boolean;
  results: TaskResult[];
  mergeResult: MergeResult;
}

// 不触碰
- supervisor-agent.js 的现有方法 (step4_execute)
- agent-executor.js
- task-dispatcher.js
```

### G2: PipelineExecutor 边界

```javascript
// 输入边界
interface G2Input {
  stages: Stage[];
  context: ExecutionContext;
}

// 输出边界
interface G2Output {
  success: boolean;
  stages: StageResult[];
  failedAt: string | null;
}

// 不触碰
- supervisor-agent.js 的现有方法 (handleTask)
- inspector.js
- quality-gate.js
```

### G3: KnowledgeBase Spec 边界

```javascript
// 输入边界
interface G3Input {
  specName: string;
  spec: SpecContent;
  options: SpecOptions;
}

// 输出边界
interface G3Output {
  path: string;
  name: string;
  reusability: number;
}

// 不触碰
- knowledge-base.js 的现有方法 (load, save, recordExecution, exportData, mergeData)
- patterns.json 格式
- metrics.json 格式
```

### G4: 外部技能注册 边界

```javascript
// 输入边界
interface G4Input {
  skillId: string;
  url: string;
}

// 输出边界
interface G4Output {
  guidance: string[];
  steps: WorkflowStep[];
}

// 不触碰
- skill-loader.js
- 现有技能文件
```

---

## 五、全量回归测试检查点

每个 Step 完成后，各端运行：

```bash
npm test
```

### G1 完成后预期

- 62 个原有测试 + 1 个新增 = 63 个测试文件
- 全部通过
- `node src/cli.js mode pipeline` 正常输出

### G2 完成后预期

- 63 + 1 = 64 个测试文件
- 全部通过
- `node src/cli.js supervisor "test" --mode pipeline` 正常执行

### G3 完成后预期

- 64 + 1 = 65 个测试文件
- 全部通过
- `node test/test-knowledge-spec.js` 6 个测试通过

### G4 完成后预期

- 65 + 1 = 66 个测试文件
- 全部通过
- `node src/cli.js clone --dry-run http://example.com` 正常输出

---

## 六、最终交付物汇总

### 新增文件（6个）

| 类型 | 文件 | 大小估算 |
|------|------|----------|
| 源码 | `src/parallel-executor.js` | ~200行 |
| 源码 | `src/pipeline-executor.js` | ~250行 |
| 测试 | `test/test-parallel-executor.js` | ~100行 |
| 测试 | `test/test-pipeline-executor.js` | ~120行 |
| 测试 | `test/test-knowledge-spec.js` | ~100行 |
| 测试 | `test/test-external-skills.js` | ~50行 |

**合计**: 2 个源码 + 4 个测试 = **6 个文件**

### 修改文件（4个）

| 文件 | 修改内容 | 改动量 |
|------|----------|--------|
| `src/supervisor-agent.js` | 新增 3 个方法 | +50行 |
| `src/cli.js` | 新增 3 个命令 | +60行 |
| `src/knowledge-base.js` | 新增 6 个方法 | +80行 |
| `.flowharness/skills/registry.json` | 新增 external 段 | +30行 |

---

## 七、同步方式

### 推荐：轻量同步

每完成一个 Step，在工作群发送确认：

```
✅ G1 ParallelExecutor 完成
- 新增: src/parallel-executor.js
- 新增: test/test-parallel-executor.js
- npm test: 全部通过
```

### 合并策略

每个 Step 完成后立即合并到 main 分支：

```bash
git checkout main
git merge feature/g1-parallel-executor --no-ff
git tag phase-g-wave1-g1-done
```

---

## 八、回滚策略

| 场景 | 回滚方式 |
|------|----------|
| 单个 Step 引入 bug | `git revert` 该 Step 的合并提交 |
| 整个 Wave 失败 | `git reset --hard phase-g-start` |
| npm test 退化 | 不允许合入，修复后重新验证 |

### 完全回滚命令

```bash
# 删除新增文件
rm src/parallel-executor.js
rm src/pipeline-executor.js
rm test/test-parallel-executor.js
rm test/test-pipeline-executor.js
rm test/test-knowledge-spec.js
rm test/test-external-skills.js
rm -rf .flowharness/knowledge/specs

# 恢复修改文件
git checkout src/supervisor-agent.js
git checkout src/cli.js
git checkout src/knowledge-base.js
git checkout .flowharness/skills/registry.json
git checkout .flowharness/config.yml
```

---

## 九、验收清单

| 检查项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| **零破坏** | `npm test` | 全部通过（66个测试） |
| **新增测试** | `npm test` | 66个测试全部通过 |
| **配置兼容** | 启动 CLI | 无报错 |
| **向后兼容** | 原有命令 | 行为不变 |
| **降级机制** | 触发失败 | 自动回退 |
| **文档完整** | 检查文件 | 5个 Step 文档存在 |

---

## 十、后续步骤

Phase G 完成后：

1. **版本发布**: 更新 package.json 版本号
2. **文档更新**: 更新 README.md
3. **CHANGELOG**: 记录本次升级内容
4. **发布公告**: 在社区发布更新信息

---

*总表完成 - 2026-04-12*
